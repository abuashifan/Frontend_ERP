# Copy to .env and adjust as needed
VITE_API_BASE_URL=http://localhost:8000

# Dev convenience: set true to enforce /login + auth guards
VITE_AUTH_ENABLED=false

# Single-company deployment: set once and keep stable
VITE_COMPANY_ID=

# Optional: enable the company switcher UI (future branch system)
VITE_TENANT_SWITCHER_ENABLED=false
